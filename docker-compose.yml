version: '3.8'

services:
  shipping-service:
    image: maven:3.9.6-eclipse-temurin-21
    container_name: dev-shipping-service
    working_dir: /app
    command: sh -c "./mvnw spring-boot:run -Dspring-boot.run.profiles=dev"
    environment:
      - SPRING_DEVTOOLS_RESTART_ENABLED=true
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    ports:
      - "8088:8080"
    networks:
      - bookstore-network

networks:
  bookstore-network:
    external: true

volumes:
  maven-repo:
    name: shared-maven-repo
